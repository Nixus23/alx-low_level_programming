#include "main.h"
/**
 * print_sign - function to output the sign of an integer
 * @n : function argument
 * Return: returns an integer depending on the value
 */
int print_sign(int n)
{
	if (n >= 1)
		_putchar('+');
		return (1);
	else if (n == 0)
		_putchar('0');
		return (0);
	else if
		_putchar('-');
		return (-1);

}
